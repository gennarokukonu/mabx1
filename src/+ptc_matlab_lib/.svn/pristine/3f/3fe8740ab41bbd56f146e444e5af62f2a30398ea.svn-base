function out = cols(matrix)
%COLS Number of columns
%   
%   SYNTAX:
%   OUT = COLS(MATRIX): Returns the number of columns in MATRIX.
%   (Same as SIZE(MATRIX, 2) for a 2D matrix)
%
%   See also: size, length, rows

out = size(matrix, ndims(matrix));

end