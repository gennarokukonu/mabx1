function outmatrix = shuffle(inmatrix)
%SHUFFLE Shuffles input vector/matrix
%   OUTMATRIX = SHUFFLE(INMATRIX)
%
%   See also: randperm

outmatrix = inmatrix(randperm(numel(inmatrix)));

outmatrix = reshape(outmatrix, size(inmatrix));

end