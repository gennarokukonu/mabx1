function out = rows(matrix)
%ROWS Number of rows
%   ROWS(MATRIX) returns the number of rows in MATRIX.
%   (Same as SIZE(MATRIX, 1))
%
%   See also size, length, cols

out = size(matrix, 1);
end
